
version: '3.8'

services:
  student-app:
    build: 
      context: ..
      dockerfile: deployment/Dockerfile
    ports:
      - "8002:8002"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ../data:/app/data
    restart: always
